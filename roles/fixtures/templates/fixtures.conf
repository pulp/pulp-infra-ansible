Header X-PyPI-Last-Serial 1000000000

RedirectMatch permanent (.+rpm.+)\?secret$ $1 

SetEnvIf Request_URI ^(.+)rpm(.+)\?secret$ let_me_in
<LocationMatch "(.+)rpm(.+)\?(.+)$">
    Order Deny,Allow
    Deny from all
    Allow from env=let_me_in
</LocationMatch>

